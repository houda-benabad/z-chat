import { StyleSheet } from 'react-native';

export const createStyles = () =>
  StyleSheet.create({
    root: {
      flex: 1,
      backgroundColor: '#000000',
    },
    content: {
      flex: 1,
    },
  });
